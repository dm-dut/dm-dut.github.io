/* Public browser configuration. Never put API keys or GitHub tokens here. */
window.PAPER_TRACKER_CONFIG = {
  localMode: true,
  refreshEndpoint: "",
  actionsUrl: "",
  refreshPollIntervalMs: 5000,
  refreshTimeoutMs: 10 * 60 * 1000,
};
