name: Paper monitor (local V6 only)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  local-mode-info:
    runs-on: ubuntu-latest
    steps:
      - name: Local mode notice
        run: |
          echo "Paper Monitor Build: LOCAL-2026.08.20-V6-ID-FIRST"
          echo "Publisher fetching is intentionally disabled on GitHub-hosted runners."
          echo "Run update_papers_scheduled.bat or update_papers.bat on the local Windows PC."
