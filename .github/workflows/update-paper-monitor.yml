name: Paper monitor (local collector only)
on:
  workflow_dispatch:
jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Paper Monitor V4 collection runs locally. Use update_papers_scheduled.bat on the Windows PC."
